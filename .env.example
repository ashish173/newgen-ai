# Database
DATABASE_URL="file:./dev.db"
APP_BASE_URL="http://localhost:3000"
NEXTAUTH_SECRET="change-me"

# SMTP (leave blank to use Ethereal in dev or JSON transport in tests)
SMTP_HOST=
SMTP_PORT=
SMTP_USER=
SMTP_PASS=
